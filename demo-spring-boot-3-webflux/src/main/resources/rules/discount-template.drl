template header
id
ifcondition
thencondition

import org.springdoc.demo.app3.model.Order;
import org.springdoc.demo.app3.model.Rule;

template "tmp1"

rule "@{id}"
  dialect "mvel"
  when
    @{ifcondition};
  then
    @{thencondition};
end

end template