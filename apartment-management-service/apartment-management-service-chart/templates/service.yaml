apiVersion: v1
kind: Service
metadata:
  name: {{ include "apartment-management-service-chart.fullname" . }}
  labels:
    {{- include "apartment-management-service-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "apartment-management-service-chart.selectorLabels" . | nindent 4 }}
