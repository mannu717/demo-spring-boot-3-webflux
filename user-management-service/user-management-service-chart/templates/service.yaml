apiVersion: v1
kind: Service
metadata:
  name: {{ include "user-management-service-chart.fullname" . }}
  labels:
    {{- include "user-management-service-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "user-management-service-chart.selectorLabels" . | nindent 4 }}
