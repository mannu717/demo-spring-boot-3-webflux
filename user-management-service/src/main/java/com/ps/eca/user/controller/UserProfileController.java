package com.ps.eca.user.controller;

public class UserProfileController {
    /**
     * User Profile Retrieval API (GET /users/{userId})
     * User Profile Update API (PUT /users/{userId})
     * Change Password API (PUT /users/{userId}/password)
     */
}
