package com.ps.eca.user.controller;

public class UserManagementController {
    /**
     * ser Deactivation API (DELETE /users/{userId})
     * User Listing API (GET /users)
     * User Role Management API (PUT /users/{userId}/roles)
     */
}
